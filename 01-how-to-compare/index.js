export const getTestPropertyIfItsDefined = object => {
    // 🐨 add logic expression which will return value at the "test" key from an object
    // if the object is not undefined. Do it in a single line using && operator
  
    return "❌";
  };
  
  export const itShouldReturnTruthynessOfGivenArgument = argument => {
    // 🐨 make sure this function returns true for truthy arguments
    // and false for falsy arguments
    // HINT: ! operator gives an opposite logic value of a thing
    // for example !0 === true, !{} === false
  
    return "❌";
  };
  
  export const returnSuccessIfArgumentIsANumber = argument => {
    // 🐨 add logic to return the message only if argument is a number
    // it can be null and undefined too! Don't use !, &&, || alone
    // when working with numbers as 0 can be a source of many bugs
  
    const message = "success";
  
    return "❌";
  };
  
  export const shouldCallConsoleLogBeforeReturningObject = object => {
    // 🐨 let's say you want to console log something before returning it
    // soooo what's the quick way you can do it by using || operator?
    // HINT: console.log(...) returns undefined
  
    return "❌";
  };
  