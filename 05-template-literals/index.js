const shouldUseVariablesInsideTheString = (a, b) => {
    // 🐨 this method should return string which match that template:
    // The value of a is VALUE_OF_A and the value of b is VALUE_OF_B

    return '❌'
}